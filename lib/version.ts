// Shown in the sidebar brand block. Convention (Tan, 2026-07-10): the minor
// number tracks the latest applied DB migration — bump it whenever a new
// migration ships so the UI tells you which schema the deploy expects.
export const APP_VERSION = 'v0.27'
